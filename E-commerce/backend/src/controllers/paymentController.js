import Order from "../models/Order.models.js";
import razorpay from '../models/Product.models.js';
import Product from '../models/Product.models.js'

// Create razorpay order
export const createOrder = async (req, res, next) => {
    try {
        
    } catch (error) {
        
    }
}